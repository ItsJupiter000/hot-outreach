Build a production-quality full-stack Job Outreach Email Bot using Next.js (App Router, TypeScript, Tailwind CSS, modern dashboard UI with sidebar navigation, reusable components, loading states, toast notifications, responsive design, and optional dark mode) for the frontend and a Node.js + Express backend inside a /server folder for API handling. The application must allow me to input a company name and company email, select an email template category (cold outreach, referral request, job application, follow-up, etc.), optionally add a custom message, preview the final rendered email, and send it using Nodemailer with SMTP credentials stored in a .env file (SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, MY_NAME, MY_EMAIL, MY_ROLE). Email templates must be stored in a local templates.json file and support dynamic variable injection using placeholders like {{companyName}}, {{myName}}, {{myRole}}, and {{customMessage}} that are replaced at send time. Every sent email must be logged in a local applications.json file (strictly no database) with a UUID, company name, email, template used, current status (default “Applied”), sent timestamp, last updated timestamp, notes field, and a history array that records all status changes with dates; also prevent duplicate sends to the same company and email within 24 hours. The backend must expose REST APIs for sending email (POST /api/send-email), fetching all applications (GET /api/applications with search and status filter support), updating status and notes (PATCH /api/applications/:id which also appends to history), and full CRUD for templates (GET/POST/DELETE /api/templates). Use a clean service layer (mailService for Nodemailer and jsonDB for safe async file read/write that auto-creates JSON files if missing), proper error handling, and validation.

The frontend must have three main pages: a Send Email dashboard page with form fields (company name, email, template dropdown populated from API, custom message textarea, live preview modal, send button, success/error toasts); an Applications tracking page with a searchable, filterable table showing company name, email, template used, status dropdown (Applied, Replied, Interview, Rejected, Offer, No Response), sent date, last updated date, editable notes, and instant API updates; and a Templates management page to list, create, and delete templates with fields for template name, subject, and HTML content editor textarea. Use UUID for IDs, TypeScript types for all data models, and keep the project well structured with folders /app, /server/routes, /server/services, /data, and shared types if needed. The app must run locally with npm run dev, include .env.example, and ship with sample templates.json and applications.json. Nice-to-have enhancements (implement if possible after core works) include bulk email sending via CSV upload, export applications to CSV, mark follow-up sent, and basic anti-spam safeguards like rate limiting. Ensure clean, maintainable, production-style code and clear setup instructions.