## Packages
date-fns | Formatting dates beautifully
clsx | Conditional class names for polished UI components
tailwind-merge | Merging tailwind classes efficiently

## Notes
- Using custom Google Fonts (Plus Jakarta Sans, Inter) for premium typography.
- Building custom beautiful Modal and Select components to ensure perfect styling and reliability.
- Assuming the backend provides the exact `api` object exported from `@shared/routes`.
- The `myName` and `myRole` variables will be rendered as placeholders in the frontend preview since they are injected via backend environment variables at send time.
